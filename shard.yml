name: html_classes
version: 0.2.0

authors:
  - Ian White <ian.w.white@gmail.com>

crystal: ">=1.6.0"

license: MIT

dependencies:
  html_class_merge:
    github: ianwhite/html_class_merge
    version: < 1
  habitat:
    github: luckyframework/habitat
    version: < 1
