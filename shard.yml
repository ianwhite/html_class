name: html_class
version: 0.3.2

authors:
  - Ian White <ian.w.white@gmail.com>

crystal: ">=1.6.0"

license: MIT

dependencies:
  html_class_merge:
    github: ianwhite/html_class_merge
    version: < 1
